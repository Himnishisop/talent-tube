import type { Payment, Report, Talent } from "./types";

// Premade demo profiles removed so only real registered talents exist in the directory.
export const SAMPLE_TALENTS: Talent[] = [];

export const SAMPLE_PAYMENTS: Payment[] = [];

export const SAMPLE_REPORTS: Report[] = [];
